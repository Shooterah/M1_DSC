package com.gameladder.gameladder;

import org.springframework.data.repository.CrudRepository;

public interface RunRepository extends CrudRepository<Run, Long> {

    /* Repository (Database tout ça) pour les Runs */
    
}
